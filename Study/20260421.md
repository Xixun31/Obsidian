[控二](控二.md) 控一是透過輸出當回授 控二是透過狀態進行回授 但全狀態回授太麻煩和昂貴了每個狀態都需要相對應的感測器 所以要用輸出去做估測來取的狀態 然後LQR $\rho\to \infty,\ \rho\to 0$ 分別是cheap control problem 和 expensive control problem 基本上就是沒控制或者控到底 啊看控制需不需要花錢 沒控制極點就不會變 控到底極點就會變到零點或漸近線 就像控一的跟軌跡一樣 LQR: Linear Quadratic Regulater $1+\rho G_{0}(-s)G_{0}(s)=0, G_{0}(s)=\frac{Z}{U}=C(sI-A)^{-1}B$

```matlab
k = lqr(A, B, Q, R)
```
